<%= form_for(@bowler) do |f| %>
  <% if @bowler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bowler.errors.count, "error") %> prohibited this bowler from being saved:</h2>

      <ul>
      <% @bowler.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :starting_lane %><br>
    <%= f.number_field :starting_lane %>
  </div>
  <div class="field">
    <%= f.label :paid %><br>
    <%= f.text_field :paid %>
  </div>
  <div class="field">
    <%= f.label :rejected %><br>
    <%= f.check_box :rejected %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
